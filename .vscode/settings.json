{
    // ========== 编辑器基础规范 ==========
    "editor.formatOnSave": true,
    "editor.formatOnSaveMode": "file",
    "editor.tabSize": 4,
    "editor.insertSpaces": true,
    "editor.rulers": [120],
    "files.insertFinalNewline": true,
    "files.trimTrailingWhitespace": true,
    "files.encoding": "utf8",
    "files.eol": "\r\n",

    // ========== 文件过滤：减少无效索引与侧边栏干扰 ==========
    "files.exclude": {
        "**/.git": true,
    },
    "search.exclude": {
        "**/build": true,
        "**/build-*": true
    },

    // ========== clangd 智能提示（核心优化） ==========
    // 使用 clangd 扩展时，必须禁用微软 C/C++ 插件的默认 IntelliSense，避免双索引卡顿、提示冲突
    "C_Cpp.intelliSenseEngine": "disabled",
    "clangd.arguments": [
        // 编译数据库位置不在此指定：CMake configure 时会自动生成项目根 .clangd，
        // 指向当前激活 preset 的构建目录（如 build/clang-debug/compile_commands.json）。
        // 注意 --compile-commands-dir 优先级高于 .clangd，若在此硬编码 build/ 会导致
        // clangd 找不到数据库、退化为无 include 路径的裸解析（gtest/gtest.h not found）。
        // 关闭 IWYU 激进头文件自动插入，避免 Windows SDK/MSVC STL 场景下误插内部头、漏宏依赖
        "--header-insertion=never",
        // 全作用域补全：无需提前写 using，直接补全全局命名空间符号
        "--all-scopes-completion",
        // 补全详情：显示完整函数签名与参数说明
        "--completion-style=detailed",
        // 预编译头存入内存，大幅提升大项目索引速度（内存≥16G 推荐）
        "--pch-storage=memory",
        // 开启 clang-tidy 静态检查（配合项目根目录 .clang-tidy 配置文件）
        "--clang-tidy",
        // 仅输出错误日志，减少输出面板噪音
        "--log=error"
    ],

    // ========== CMake 配置（与 Presets 无冲突） ==========
    "cmake.configureOnOpen": true,
    // 构建目录：仅作为无 Preset 时的默认值，优先以 CMakePresets.json 中 binaryDir 为准
    "cmake.buildDirectory": "${workspaceFolder}/build",
    // 生成编译数据库，clangd 必需
    "cmake.configureArgs": [
        "-DCMAKE_EXPORT_COMPILE_COMMANDS=ON"
    ],
    // 并行编译核数：自动匹配 CPU 核心数，也可手动写数字如 "8"
    "cmake.parallelJobs": 0
}
